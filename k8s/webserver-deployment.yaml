apiVersion: apps/v1
kind: Deployment
metadata:
  name: webserver
  namespace: mern-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: webserver
  template:
    metadata:
      labels:
        app: webserver
    spec:
      containers:
      - name: webserver
        image: <yourdockerhub>/mern_webserver:latest
        ports:
        - containerPort: 5000
        env:
        - name: MONGO_URI
          value: "mongodb://mongo-service:27017/mernapp"
